apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Choose exactly one network graph. This example uses the shared-router design.
resources:
  - identity-config.yaml
  - identity.yaml
  - spoke-config.yaml
  - network-dedicated.yaml
  - cluster.yaml
  - hello-app.yaml
