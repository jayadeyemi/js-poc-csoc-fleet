apiVersion: csoc.js2.org/v1alpha1
kind: SpokeCluster
metadata:
  name: poc-tenant-dev
  namespace: spokeclusters-test-poc
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  kubernetes:
    minNodes: 1
    maxNodes: 2
