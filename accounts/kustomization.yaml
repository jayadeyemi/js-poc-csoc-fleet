apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Replacement gate: keep the account manifests tracked but inactive while the
# current immutable identity, cluster, and network are retired in order.
resources: []
